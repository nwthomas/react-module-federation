import * as React from 'react';

import App from './App.js';
import ReactDOM from 'react-dom';

const rootElement = docyment.getElementById('mobile-dev-root');

ReactDOM.render(<App />, rootElement);
